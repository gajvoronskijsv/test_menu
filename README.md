Запускаеься в docker-compose с nginx и пустой БД (создавать суперюзера в контейнере). 
Можно переключить в настройках проекта на sqlite и запустить через runserver, чтобы посмотреть пример.
Функционал реализован без сторонних библиотек, gunicorn и sqlclient нужны только для запуска докером.